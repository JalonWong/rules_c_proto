bcr_test_module:
  module_path: ''
  matrix:
    platform:
      - debian10
      - ubuntu2204
      - macos
      - windows
    bazel: [8.x]
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
      - '//test:tests'
